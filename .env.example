# OpenAI
OPENAI_API_KEY=sk-xxx
OPENAI_REALTIME_MODEL=gpt-4o-realtime-preview

# MCP (Zapier) – server-side config
# Toggle in UI via useTools checkbox. Server injects tool when these are set.
ZAPIER_MCP_SERVER_URL=https://mcp.zapier.com/api/mcp/mcp
ZAPIER_MCP_LABEL=zapier
ZAPIER_MCP_REQUIRE_APPROVAL=never
# Optional bearer used for Authorization header to Zapier MCP
ZAPIER_MCP_TOKEN=replace-with-your-token
